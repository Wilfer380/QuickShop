<x-app-layout><div class="crud-page"><section class="crud-hero"><div><span class="crud-eyebrow">Detalle tarifa</span><h1>{{ $tarifa->nombre }}</h1><p>{{ $tarifa->tipo_vehiculo }} - {{ $tarifa->tipo_cobro }}</p></div><div class="crud-actions"><a class="crud-link" href="{{ route('tarifas.edit', $tarifa) }}">Editar</a><a class="crud-link" href="{{ route('tarifas.index') }}">Volver</a></div></section><section class="crud-panel">@if (session('status'))<div class="crud-alert">{{ session('status') }}</div>@endif<div class="crud-detail"><div><span>Valor</span><strong>${{ number_format((float) $tarifa->valor, 2) }}</strong></div><div><span>Estado</span><strong>{{ $tarifa->activa ? 'Activa' : 'Inactiva' }}</strong></div><div><span>Tipo vehiculo</span><strong>{{ $tarifa->tipo_vehiculo }}</strong></div><div><span>Descripcion</span><strong>{{ $tarifa->descripcion ?? 'Sin descripcion' }}</strong></div></div></section></div><x-admin-crud-styles /></x-app-layout>
