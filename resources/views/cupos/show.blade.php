<x-app-layout><div class="crud-page"><section class="crud-hero"><div><span class="crud-eyebrow">Detalle cupo</span><h1>{{ $cupo->codigo }}</h1><p>{{ $cupo->zona ?? 'Sin zona' }} - {{ $cupo->estado }}</p></div><div class="crud-actions"><a class="crud-link" href="{{ route('cupos.edit', $cupo) }}">Editar</a><a class="crud-link" href="{{ route('cupos.index') }}">Volver</a></div></section><section class="crud-panel">@if (session('status'))<div class="crud-alert">{{ session('status') }}</div>@endif<div class="crud-detail"><div><span>Tipo vehiculo</span><strong>{{ $cupo->tipo_vehiculo }}</strong></div><div><span>Estado</span><strong>{{ $cupo->estado }}</strong></div><div><span>Zona</span><strong>{{ $cupo->zona ?? 'Sin zona' }}</strong></div><div><span>Observaciones</span><strong>{{ $cupo->observaciones ?? 'Sin observaciones' }}</strong></div></div></section></div><x-admin-crud-styles /></x-app-layout>
